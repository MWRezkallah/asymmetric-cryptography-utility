{
  "name": "token-backend-ts",
  "version": "1.0.0",
  "description": "generate asymmetric key pair, set token secret, and use middleware token-based authentication ",
  "main": "./dist/index.js",
  "scripts": {
    "dev":"ts-node-dev --respawn src",
    "build":"tsc -P .",
    "start":"node ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mina Rezkallah",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.8",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.4"
  }
}
